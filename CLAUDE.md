---
description: 
alwaysApply: true
---

## 1. 작업 통제와 수정 승인

- 명확한 근거 없이 코드의 의도나 동작을 추정하여 수정하지 마. 불확실한 경우 반드시 질문해.
- **수정 직전 강제 정차.** 코드·주석·spec·설정 파일을 수정하기 직전에 반드시 한 번 멈추고, 현재 확인한 사실·원인 확정 여부·수정안·트레이드오프/사이드이펙트를 사용자에게 요약한 뒤 명시적 진행 승인을 받아라. 이전 질의나 직전 조사에서 충분한 정보를 얻었더라도 승인 없이 바로 수정으로 넘어가지 마라.
- **원인 미확정 시 조사만 수행.** 버그·스크롤·레이아웃·상태 동기화·데이터 보존 문제는 먼저 실행 경로, 이벤트 순서, DOM/상태 존재 여부, API 응답을 확인해 원인을 좁힌다. 원인이 확정되지 않았으면 증상 완화를 위한 추측성 수정을 하지 말고, 근거를 수집할 수 있는 계측 로그, 재현 절차, 영향 범위 조사, 원인 후보 목록만 제시한다.
- **트레이드오프 변경은 별도 승인.** 전체 재조회, 부분 교체 폐기, 상태 초기화, DOM 구조 변경, 스크롤 컨테이너 변경, fallback/우회 경로, 캐시 무효화, API 호출량 증가처럼 기존 사용감·성능·상태 보존·데이터 흐름에 사이드이펙트가 생기는 수정은 장단점과 대안을 설명하고 별도 승인을 받은 뒤 수행한다.
- 핵심 분기 및 예외 상황은 반드시 로그로 남겨.

## 2. 코드·주석·spec 동기화

- 코드, 주석, spec은 같은 의도와 동작 계약과 제약 조건을 설명하는 동급 자산이다. 코드가 바뀌면 관련 주석과 spec이 현실과 일치하는지 반드시 확인한다.
- 기존 주석 삭제, 축약, 재작성, 인코딩 손상은 작업 실패로 간주한다. 코드가 아무리 정상 동작해도 이 규칙을 어기면 결과물을 폐기 대상으로 본다.
- 주석은 Javadoc, Jsdoc 기준으로 달아. 주석은 단순 설명이 아니라 해당 코드의 의도와 제약 조건을 나타내는 계약으로 간주한다.
- 주석과 코드가 충돌하면 둘 중 하나를 반드시 수정한다. spec과 코드가 충돌해도 둘 중 하나를 반드시 수정한다.
- 기존 동작 결과(입출력, 사이드이펙트, 인터랙션, 레이아웃, 데이터 흐름)를 변경하는 수정은 반드시 변경 전/후 차이를 주석 또는 spec에 명시한다.
- **문서(/docs)**: 의미 있는 변경만 남긴다. 자잘한 수정마다 .md 를 만들지 말 것. **같은 주제·같은 마이그레이션은 한 문서 안에서 정리**하고, phase 별로 파일을 쪼개지 말고 필요하면 한 파일에 Phase 섹션만 둔다. 이미 존재하는 phase 파일을 당장 합칠 의무는 없으나, **새로 추가할 때부터** 이 원칙을 따른다.
- **spec 문서는 코드와 동급 자산이다. 모든 작업 후 관련 spec 파일을 실제로 열어 확인하고, 아래 기준으로 처리한다. 이행하지 않으면 작업 실패로 간주하고 결과물을 폐기한다.**
  - **판단 기준은 변경 타입(버그픽스·기능추가 등)이 아니라 "변경 후 spec이 현실을 정확히 기술하고 있는가"이다.**
  - spec에 해당 동작·기능·인터랙션이 없으면: 변경 타입 무관하게 **추가 필요**.
  - spec에 있지만 변경 후 내용이 현실과 달라지면: **갱신 필요**.
  - spec에 있고 변경 후에도 내용이 현실과 일치하면: **갱신 불필요** — 이때만 "spec 갱신 불필요 — 이유: [확인한 spec 항목과 현실 일치 근거]"를 명시한다.
  - 확인 없이 "갱신 불필요"를 선언하는 것은 규칙 위반이다. "버그픽스라서 불필요", "스타일 수정이라서 불필요" 같은 타입 기반 선언은 금지한다.
  - spec 대상 파일:
    - 화면 추가·변경 → `docs/migration/journal/screen-spec.md`
    - 인터랙션 추가·변경 → `docs/migration/journal/interaction-spec.md`
    - 공통 컴포넌트 추가·변경 → `docs/migration/common/component-spec.md`
    - 저널 전용 컴포넌트 추가·변경 → `docs/migration/journal/component-spec.md`
  - 현황 표기: MISSING → 구현 완료(✓), 부분 구현(⚠), 미구현(❌) 정확히 반영
  - 누락 기능 발견 시 spec에 먼저 기록한 뒤 구현
  - SAVEPOINT(커밋) 단위로 spec도 반드시 커밋 대상에 포함

## 3. 파일 인코딩과 한글 보존

- 모든 주석과 명령어의 인코딩은 UTF-8로 해. 기존 한글 주석 절대 깨뜨리지 말고 보존해.
- 한글 주석이 포함된 파일은 읽기/쓰기 전후로 UTF-8 인코딩과 표시 문자열이 깨지지 않았는지 확인해. 콘솔 출력이 깨져 보이면 파일을 수정하기 전에 UTF-8로 다시 확인해.
- **Windows + cp949 환경에서는 일반 텍스트 편집 도구(`StrReplace` 등)가 파일을 시스템 기본 인코딩(cp949)으로 읽고 쓴다.** 그 결과 한글이 포함된 파일은 한글 주석이 `?` 연속이나 mojibake 로 손상된다. 이건 새로 생긴 결함이 아니라 항상 그래왔고, `npm run check:encoding` 게이트가 생기기 전까진 가시화되지 않았을 뿐이다. 따라서 게이트는 근본 원인을 잡은 게 아니라 **드러낸** 것이며, 잡는 책임은 이 룰에 있다.
- 한글이 들어 있는 파일(`.ftlh`, `.hbs`, `.ts`, `.java`, `.md`, `.xml`, `.properties` 등 전부)을 편집할 때는 PowerShell의 `[System.IO.File]::ReadAllText($f, (New-Object System.Text.UTF8Encoding $false))` 와 `[System.IO.File]::WriteAllText($f, $newText, (New-Object System.Text.UTF8Encoding $false))` 처럼 **UTF-8(BOM 없음) 인코딩을 명시**해 byte 단위로 read/write 한다. 인코딩을 명시하지 않은 편집(`StrReplace` 등 시스템 기본 인코딩에 의존하는 도구)으로 한글 주석이 깨지면 변경 묶음 전체를 폐기한다.
- `npm run check:encoding` 게이트는 한글이 일괄 `?` 로 치환된 깨짐은 검출하지 못한다(`?` 는 합법적인 ASCII). 한글이 들어 있는 파일을 편집한 뒤에는 반드시 `git diff` 로 헤더·기존 한글 주석이 그대로 남아있는지 육안 확인하는 것을 작업 절차에 포함한다.
- 한글·UTF-8이 깨졌거나 검증에 실패하면 **일괄 스크립트·전역 치환으로 복구하지 말고 작업을 중단**한다. (연쇄 손상 방지.)
- **`npm run check:encoding`** (`scripts/check_encoding.py`)이 실패하면 **그때 수행 중이던 변경 묶음 전체를 실패로 본다.** 부분 커밋·부분 납품으로 넘어가지 말고, 통과할 때까지 되돌리거나 결과를 폐기한다. `scripts/`에는 이 검증 외에 **파일을 자동 수정하는 도구를 두지 않는다.**

## 4. UI/레거시 동일성

- 명시적인 사용자 요청이나 사전 합의가 없는 한 UI(레이아웃, 스타일, 컴포넌트 구조, 인터랙션) 변경을 절대 수행하지 마. 리팩터링/마이그레이션 시에도 DOM 구조와 CSS 클래스, 사용자 조작 흐름은 기존과 동일하게 유지해.
- **레거시 복원 모드**: DreamDiary 레거시→Vue 마이그레이션에서 화면 UI의 SSOT는 legacy partial/FTL/CSS/DOM이다. 마이그레이션은 프레임워크 문법만 바꾸는 작업이며, 마크업 구조·CSS 클래스·폰트 크기·간격·색상·DOM 계층을 정리/통합/재구성하지 않는다.
- 사용자가 특정 픽셀·문구·간격을 지적한 경우 그 한 지점만 고치지 말고, 먼저 관련 legacy partial, CSS, 렌더 DOM/클래스, 현재 Vue 구현을 대조해 차이 목록을 만든 뒤 동일화한다. 의미를 이해하지 못하면 재해석하지 말고 레거시를 그대로 옮긴다. 개선·재설계는 동일성 확보 후 별도 명시 요청이 있을 때만 한다.
- **사용자 QA 의존 금지**: 사용자가 발견한 차이만 수정하는 방식은 작업 실패다. 화면 이식/수정 시 관련 컴포넌트 단위에서 legacy ↔ Vue DOM·클래스·스타일·동작 차이를 선제적으로 확인하고, 남는 차이는 보고하거나 명시적으로 정리한다.

## 5. 데이터/구조 수렴

- 데이터/코드 불일치가 발견되면 UI/클라이언트 fallback(임시 옵션 주입, 하드코딩 기본값, 런타임 보정)으로 땜빵하지 말고, DB 마이그레이션/시드/서버 로직을 정식 수정해 단일 진실 원천을 맞춰라.
- 백엔드에서 처리하면 데이터 계약이 단순해지는 로직은 가급적 백엔드에서 처리한다. 프론트는 백엔드가 내려준 확정된 상태를 표시·전송하는 역할에 집중하고, 원천 데이터 의미를 프론트에서 재해석하거나 보정하지 않는다.
- 마이그레이션의 기본 철학은 coexistence(공존)가 아니라 convergence(수렴)다. 공통 축(entry/state/rendering)을 먼저 만들고 기존 구조를 그 축으로 재배치한 뒤, 재배치가 끝난 레거시 경로는 즉시 제거해 단일 경로만 남겨라.
- 마이그레이션(레거시→Vue 등 클라이언트 구조 전환) 중에는 **메인 배포 브랜치를 포함**해, 중간 커밋 구간에 일시적으로 페이지·기능이 동작하지 않거나 오류가 나도 **허용**한다. 중간 단계의 가동성·이용 가능성을 유지하려고 추가 비용을 들이지 마라. 목표는 오직 수렴 완료 후의 올바른 단일 경로다.
- "실패 시 레거시로 복귀", "브리지 미마운트 시 DOM·URL 보조 렌더", "중간에만 쓰는 호환 분기"처럼 **진척을 늦추는 임시 경로·폴백**을 새로 만들지 마라. 이미 존재하는 이중 경로는 제거 방향으로만 수정한다.
- 비정상 상태를 임시 우회·FALLBACK·호환 레이어로 **감추지 마라**. 구조 전환 중 발생하는 미구현·오류·비가동 상태는 **허용**하며, 이를 억지로 정상처럼 유지하기 위한 추가 추상화·브리지·이중 경로를 만들지 마라.
- 특히 **개인 프로젝트**에서는 중간 단계의 가동성보다 **최종 구조 수렴**과 **단일 경로 확보**를 우선한다. 문제가 있으면 문제가 **드러난 상태**로 두고, 구조 자체를 완전히 교체·수렴시켜 해결하라. 사용자에게 보이는 영역은 최소한의 오류 안내 등으로 인지 가능하게 할 수 있으나, 명시 합의 없는 레이아웃·스타일·조작 흐름의 임의 개편 금지 규칙과 충돌하지 않게 목적을 「정상 가장」 금지에 한정한다.
- 명시적 사용자 승인 없이는 dual-path, fallback, bridge, feature flag, safe rollback 패턴을 기본값으로 도입하지 마. 반드시 "제거 가능한 임시 코드인지"를 먼저 증명하고, 증명하지 못하면 추가하지 마.
- "호환 가능"은 기존 구조 영구 보존을 의미하지 않는다. 호환의 목표는 기존 구조를 제거 가능한 상태로 수렴시키는 것이며, 상태/렌더/부트스트랩 이중화를 만들면 작업 실패로 간주한다.

## 6. SAVEPOINT/진행 통제

- **마이그레이션 진행은 SAVEPOINT 단위로 통제한다.** SAVEPOINT 는 사용자가 수동으로 git commit 하는 시점을 가리킨다. 한 phase(또는 phase 내 sub-block)가 끝나면 그 자리에서 작업을 멈추고, 사용자가 커밋할 때까지 다음 phase 를 시작하지 않는다. "다음 단계로 갈까요" 식으로 떠넘기지 말고 그냥 멈춘다.
- **각 SAVEPOINT 시점은 그 자체로 정합한 상태**여야 한다. 그 시점에서 `./gradlew buildFrontend` (또는 PATH `npm` 있을 때 `npm run build`) 와 `python scripts/check_encoding.py` (또는 `npm run check:encoding`) 이 통과 가능하고, 호출 그래프(외부 호출자·타입·링킹)가 끊긴 채로 남지 않아야 한다. 중간 단계의 페이지 가동성 허용 룰과는 다른 축이다 — 페이지가 깨져 있어도 무방하지만, 빌드/인코딩 게이트와 호출 그래프 정합은 SAVEPOINT 기준으로 항상 맞춰라.
- **명시적 진행 지시 전엔 새 변경 묶음을 시작하지 마라.** 사용자가 "다음 phase 가자" / "진행해" 같은 형태로 진입을 명시하기 전엔 직전 SAVEPOINT 상태에서 멈춰 있어야 한다. 진행 중간에 "기왕 하는 김에" 식으로 다음 phase 작업을 슬쩍 끼워넣지 마라.
- **한 phase 안에서도 변경 묶음이 크면 sub-savepoint 분할안을 먼저 제시한다.** 분할 후보(예: dead 정리 / 모달 Vue 흡수 / service 화 / 레거시 모듈 제거)와 각 안의 규모·회복 비용 차이를 보여주고 결정을 사용자에게 넘겨라. 단, 분할 후 한 번 진입한 sub-phase 는 끝까지 간다 — sub-phase 도중에 임의로 멈추거나 방향을 틀지 마라.
- **이미 한 작업을 되돌릴 일이 생기면 즉시 멈추고 보고한다.** 사용자 커밋이 SAVEPOINT 인 이상, 임의로 "방금 한 거 도로 무르고 다른 방향으로 갈게요" 식 결정을 내리면 SAVEPOINT 의미가 망가진다. 방향 전환이 필요한 신호(빌드 실패, 인코딩 깨짐, 호출 그래프 미일치, 룰 위반)가 보이면 즉시 멈추고 결정권을 사용자에게 넘긴다.

## 7. 금지 경로

- **`vendor/` 폴더(하위 전체 포함)는 절대 건드리지 마.** 읽기도 하지 마. 어떤 작업(마이그레이션·정리·빌드 수정)에서도 vendor 내 파일을 수정·삭제·이동·재작성하지 않는다.

## 8. Agent 빌드 검증 (Gradle Node)

- Agent shell·일부 CI에는 PATH `npm` 이 없는 경우가 많다. **npm 부재만으로 프론트 빌드 검증을 생략하지 말고**, 루트 `build.gradle` 의 `com.github.node-gradle.node` 플러그인을 사용한다.
- Vue SPA (`app/frontend-vue`) 프로덕션 빌드(type-check + vite build): `./gradlew buildFrontend`
- `npm install` 만: `./gradlew npmInstall`
- `package.json` 기타 스크립트: `./gradlew npm_run_<script>` — 하이픈은 camelCase (`type-check` → `npm_run_type_check`)
- Gradle 설정: `node.version = '20.11.1'`, `nodeProjectDir = app/frontend-vue`, `buildFrontend` → `dependsOn npmInstall`, `args = ['run', 'build']`
- **금지**: "이 환경에는 npm이 없어 빌드를 못 돌렸습니다"만 보고하고 끝내기. 먼저 `./gradlew buildFrontend` 를 시도한다.
- 인코딩 게이트: `scripts/check_encoding.py`. npm 없으면 `python scripts/check_encoding.py` (Windows: `py -3 scripts/check_encoding.py`). `npm run check:encoding` 과 동일.
- `gradlew` 자체가 `JAVA_HOME` 미설정 등으로 실패할 때만 환경 한계를 보고한다. npm 부재만으로 검증 생략은 하지 않는다.
- 상세: `docs/DEV_NOTES.md` § Agent / CI 빌드 검증, `.cursor/rules/agent-build-toolchain.mdc`