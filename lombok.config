# 프로젝트 밖의 사용자별 Lombok 설정을 상속하지 않는다.
config.stopBubbling = true

# Lombok 1.18.34 이전과 동일하게 생성 코드를 JaCoCo 측정 대상에 유지한다.
lombok.addLombokGeneratedAnnotation = false

# Lombok 1.18.40 이전의 Jackson annotation accessor 복사 계약을 보존한다.
lombok.copyJacksonAnnotationsToAccessors = true
