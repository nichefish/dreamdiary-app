<config xmlns:xsi='http://www.w3.org/2001/XMLSchema-instance'
        xmlns='http://www.ehcache.org/v3'
        xmlns:jsr107='http://www.ehcache.org/v3/jsr107'
        xsi:schemaLocation="http://www.ehcache.org/v3
                            http://www.ehcache.org/schema/ehcache-core-3.7.xsd
                            http://www.ehcache.org/v3/jsr107
                            http://www.ehcache.org/schema/ehcache-107-ext-3.7.xsd">

    <cache-template name="defaultTemplate">
        <!-- 罹먯떆 留뚮즺湲곌컙 ?ㅼ젙 -->
        <expiry>
            <!-- unit : nanos, micros, millis, seconds, minutes, hours, days -->
            <ttl unit="days">1</ttl>
        </expiry>
        <resources>
            <!-- 罹먯떆??理쒕? 紐?媛쒓퉴吏 ??ν븷吏 ?ㅼ젙 -->
            <heap unit="entries">9999</heap>
        </resources>
    </cache-template>

    <!-- alias??@Cacheable(value = "codes", key="#codeGrp")?먯꽌 value 媛믪쑝濡??ъ슜 -->

    <cache alias="defaultCache">
        <expiry>
            <ttl unit="seconds">3600</ttl>
        </expiry>
        <resources>
            <heap unit="entries">1000</heap>
        </resources>
    </cache>

    <!-- ?? -->

    <!-- ??? ?? -->
    <cache alias="auditorInfo" uses-template="defaultTemplate" />

    <!-- ?? ?? ?? -->
    <cache alias="authPolicy" uses-template="defaultTemplate" />
    <cache alias="authPolicyEntity" uses-template="defaultTemplate" />

    <!-- 硫붾돱 -->
    <cache alias="userMenuList" uses-template="defaultTemplate" />
    <cache alias="mngrMenuList" uses-template="defaultTemplate" />
    <cache alias="menuByLabel" uses-template="defaultTemplate" />
    <cache alias="isMngrMenu" uses-template="defaultTemplate" />

    <!-- ??? -->
    <cache alias="boardMenuList" uses-template="defaultTemplate" />
    <cache alias="boardMenu" uses-template="defaultTemplate" />

    <!-- ?? ?? -->
    <cache alias="codeItemCodeName" uses-template="defaultTemplate" />

    <!-- ?? ?? -->
    <cache alias="holydayEntityList" uses-template="defaultTemplate" />
    <cache alias="holydayMap" uses-template="defaultTemplate" />
    <cache alias="isHolyday" uses-template="defaultTemplate" />
    <cache alias="isHolydayOrWeekend" uses-template="defaultTemplate" />

    <!-- ?? ?? -->

    <!-- ?? ?? -->
    <cache alias="journalDayYyMnthListByUser" uses-template="defaultTemplate" />
    <cache alias="journalDayWeeklyListByUser" uses-template="defaultTemplate" />
    <cache alias="journalDayDtlDtoByUser" uses-template="defaultTemplate" />
    <!-- ?? ??: ?? -->
    <cache alias="journalDayPeriodTagListByUser" uses-template="defaultTemplate" />
    <cache alias="journalDayTagYyListByUser" uses-template="defaultTemplate" />
    <cache alias="journalDayTagCtgrMapByUser" uses-template="defaultTemplate" />
    <cache alias="journalDayMetaCtgrMapByUser" uses-template="defaultTemplate" />
    <cache alias="journalDayMetaYyListByUser" uses-template="defaultTemplate" />
    <cache alias="journalDayTagCountMapByUser" uses-template="defaultTemplate" />

    <!-- ?? ?? -->
    <cache alias="journalChapterDetailDtoByUser" uses-template="defaultTemplate" />
    <cache alias="journalChapterStateMapByUser" uses-template="defaultTemplate" />
    <cache alias="journalChapterWeeklyStateMapByUser" uses-template="defaultTemplate" />

    <!-- ?? ?? -->
    <cache alias="journalEntryDtlDtoByUser" uses-template="defaultTemplate" />
    <cache alias="journalEntryYyAnnualStatedListByUser" uses-template="defaultTemplate" />
    <cache alias="journalEntryTagListByUser" uses-template="defaultTemplate" />
    <cache alias="journalEntryPeriodTagListByUser" uses-template="defaultTemplate" />
    <cache alias="journalEntryTagCtgrMapByUser" uses-template="defaultTemplate" />
    <cache alias="journalEntryTagCountMapByUser" uses-template="defaultTemplate" />
    <cache alias="journalDiaryStateMapByUser" uses-template="defaultTemplate" />
    <cache alias="journalDiaryWeeklyStateMapByUser" uses-template="defaultTemplate" />

    <!-- ?? ?? -->
    <cache alias="journalNoteStateMapByUser" uses-template="defaultTemplate" />
    <cache alias="journalNoteWeeklyStateMapByUser" uses-template="defaultTemplate" />

    <!-- ?? ? -->
    <cache alias="journalDreamStateMapByUser" uses-template="defaultTemplate" />
    <cache alias="journalDreamWeeklyStateMapByUser" uses-template="defaultTemplate" />

    <!-- ?? ?? -->
    <cache alias="journalInterpretationDetailDtoByUser" uses-template="defaultTemplate" />
    <cache alias="journalInterpretationStateMapByUser" uses-template="defaultTemplate" />
    <cache alias="journalInterpretationWeeklyStateMapByUser" uses-template="defaultTemplate" />

    <!-- ?? ?? -->
    <cache alias="journalTodoListByUser" uses-template="defaultTemplate" />
    <cache alias="journalTodoDetailDtoByUser" uses-template="defaultTemplate" />

    <!-- ?? ?? -->
    <cache alias="journalAnnualTotalListByUser" uses-template="defaultTemplate" />
    <cache alias="journalAnnualListByUser" uses-template="defaultTemplate" />
    <cache alias="journalAnnualDetailDtoByUser" uses-template="defaultTemplate" />
    <cache alias="journalAnnualYyDetailDtoByUser" uses-template="defaultTemplate" />

    <!-- ?? -->
    <cache alias="tagContentEntityListByRef" uses-template="defaultTemplate" />
    <cache alias="tagSmpDtlEntity" uses-template="defaultTemplate" />

    <!-- ?? -->
    <cache alias="metaContentEntityListByRef" uses-template="defaultTemplate" />
    <cache alias="metaSmpDtlEntity" uses-template="defaultTemplate" />
</config>
