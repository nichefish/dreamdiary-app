<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.nicheblog.dreamdiary.feature.journal.chapter.repository.mybatis.JournalChapterMapper">

    <resultMap id="JournalChapterMap" type="io.nicheblog.dreamdiary.feature.journal.chapter.model.JournalChapterDto">
        <id property="id" column="id"/>
        <result property="createdBy" column="created_by"/>
        <result property="journalDayId" column="journal_day_id"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="yy" column="yy"/>
        <result property="mnth" column="mnth"/>
    </resultMap>

    <!-- 삭제 데이터 조회 -->
    <select id="getDeletedById" resultMap="JournalChapterMap">
        /* JournalChapterMapper.getDeletedById */
        SELECT
            chapter.id,
            chapter.created_by,
            chapter.journal_day_id,
            chapter.sort_order,
            day.yy,
            day.mnth
        FROM journal_chapter chapter
        LEFT JOIN journal_day day
            ON chapter.journal_day_id = day.id
        WHERE chapter.id = #{id}
    </select>

</mapper>

