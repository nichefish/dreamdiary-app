<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.nicheblog.dreamdiary.feature.jrnl.chapter.repository.mybatis.JrnlChapterMapper">

    <resultMap id="JrnlChapterMap" type="io.nicheblog.dreamdiary.feature.jrnl.chapter.model.JrnlChapterDto">
        <id property="id" column="id"/>
        <result property="createdBy" column="created_by"/>
        <result property="jrnlDayId" column="jrnl_day_id"/>
        <result property="idx" column="idx"/>
        <result property="yy" column="yy"/>
        <result property="mnth" column="mnth"/>
    </resultMap>

    <!-- 삭제 데이터 조회 -->
    <select id="getDeletedById" resultMap="JrnlChapterMap">
        /* JrnlChapterMapper.getDeletedById */
        SELECT
            chapter.id,
            chapter.created_by,
            chapter.jrnl_day_id,
            chapter.idx,
            day.yy,
            day.mnth
        FROM jrnl_chapter chapter
        LEFT JOIN jrnl_day day
            ON chapter.jrnl_day_id = day.id
        WHERE chapter.id = #{id}
    </select>

    <!-- 인덱스 갱신을 위한 전체 객체 조회 -->
    <select id="findAllForReorder" resultMap="JrnlChapterMap">
        /* JrnlChapterMapper.findAllForReorder */
        SELECT *
        FROM jrnl_chapter
        WHERE jrnl_day_id = #{jrnlDayId} AND deleted_at IS NULL
        ORDER BY idx
    </select>

    <!-- 인덱스 갱신 -->
    <update id="batchUpdateIdx">
        /* JrnlChapterMapper.batchUpdateIdx */
        <foreach collection="list" item="item" separator=";">
            UPDATE jrnl_chapter
            SET idx = #{item.idx}
            WHERE id = #{item.id} AND deleted_at IS NULL
        </foreach>
    </update>
</mapper>
