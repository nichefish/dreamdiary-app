<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.nicheblog.dreamdiary.feature.jrnl.dream.repository.mybatis.JrnlDreamMapper">

    <resultMap id="JrnlDreamMap" type="io.nicheblog.dreamdiary.feature.jrnl.dream.model.JrnlDreamDto">
        <id property="id" column="id"/>
        <result property="regstrId" column="regstr_id"/>
        <result property="jrnlDayId" column="jrnl_day_id"/>
        <result property="idx" column="idx"/>
        <result property="yy" column="yy"/>
        <result property="mnth" column="mnth"/>
    </resultMap>

    <!-- 삭제 데이터 조회 -->
    <select id="getDeletedById" resultMap="JrnlDreamMap">
        /* JrnlDreamMapper.getDeletedById */
        SELECT
            dream.id,
            dream.regstr_id,
            dream.jrnl_day_id,
            dream.idx,
            day.yy AS yy,
            day.mnth AS mnth
        FROM jrnl_dream dream
        LEFT JOIN jrnl_day day
            ON dream.jrnl_day_id = day.id
        WHERE dream.id = #{id}
    </select>

    <!-- 인덱스 갱신을 위한 전체 객체 조회 -->
    <select id="findAllForReorder" resultMap="JrnlDreamMap">
        /* JrnlDreamMapper.findAllForReorder */
        SELECT *
        FROM jrnl_dream
        WHERE jrnl_day_id = #{jrnlDayId} AND del_yn = 'N'
        ORDER BY idx
    </select>

    <!-- 인덱스 갱신 -->
    <update id="batchUpdateIdx">
        /* JrnlDreamMapper.batchUpdateIdx */
        <foreach collection="list" item="item" separator=";">
            UPDATE jrnl_dream
            SET idx = #{item.idx}
            WHERE id = #{item.id} AND del_yn = 'N'
        </foreach>
    </update>
</mapper>
